version: '3.8'

services:
  vault:
    image: hashicorp/vault:latest
    container_name: vault
    ports:
      - "8200:8200"
    volumes:
      - ./vault_setup/vault.hcl:/etc/vault.d/vault.hcl
      - ./vault_setup/tls:/opt/vault/tls
      - ./vault_setup/data:/opt/vault/data
    cap_add:
      - IPC_LOCK
    environment:
      VAULT_ADDR: https://vault.local:8200
      VAULT_CACERT: /opt/vault/tls/tls.crt
